//
// auto-generated by op2.py
//

void CalcHalfStepBVC_omp4_kernel(
  double *data0,
  int dat0size,
  double *data1,
  int dat1size,
  double *data2,
  int dat2size,
  int count,
  int num_teams,
  int nthread){

  #pragma omp target teams num_teams(num_teams) thread_limit(nthread) map(to:data0[0:dat0size],data1[0:dat1size],data2[0:dat2size]) \
    map(to: m_c1s_ompkernel)
  #pragma omp distribute parallel for schedule(static,1)
  for ( int n_op=0; n_op<count; n_op++ ){
    //variable mapping
    double *bvc = &data0[1*n_op];
    const double *compression = &data1[1*n_op];
    double *pbvc = &data2[1*n_op];

    //inline function
    
      bvc[0] = m_c1s_ompkernel * (compression[0] + double(1.));
      pbvc[0] = m_c1s_ompkernel;
    //end inline func
  }

}
